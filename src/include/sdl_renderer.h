#ifndef SDL_RENDERER_H
#define SDL_RENDERER_H

#include <SDL.h>

// Declare window and renderer as external so they can be accessed from other files.
extern SDL_Window* g_window;
extern SDL_Renderer* g_renderer;

#endif // SDL_RENDERER_H
